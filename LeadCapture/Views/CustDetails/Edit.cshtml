@model LeadCapture.Models.CustDetail

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>CustDetail</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Title)

        @Html.HiddenFor(model => model.FirstName)

        @Html.HiddenFor(model => model.LastName)

        @Html.HiddenFor(model => model.MobileNumber)

        @Html.HiddenFor(model => model.AlternativeNumber)

        @Html.HiddenFor(model => model.EmailAddress)

        @Html.HiddenFor(model => model.PostCode)

        @Html.HiddenFor(model => model.Housenameornumber)

        @Html.HiddenFor(model => model.LooingFor)

        @Html.HiddenFor(model => model.ValueOfTheProperty)

        @Html.HiddenFor(model => model.MorttgageAmount)

        @Html.HiddenFor(model => model.Term)

        @Html.HiddenFor(model => model.TypeOfRate)

        @Html.HiddenFor(model => model.MissedEMI)

        @Html.HiddenFor(model => model.Id)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script src="~/Scripts/jquery.validate.min.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>
